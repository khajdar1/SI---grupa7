# MySQL init scripts

Ovaj folder je predviđen za početne SQL skripte koje se mogu automatski izvršiti pri startu MySQL kontejnera kroz `docker-compose.yml`.

Za sada je namjerno prazan jer se šema i seed podaci još ne isporučuju kroz zasebne init skripte.