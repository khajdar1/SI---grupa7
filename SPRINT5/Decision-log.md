# Sprint 5 - Decision Log

| ID odluke | Datum | Naziv odluke | Opis | Razmatrane opcije | Odabrana opcija | Razlog izbora | Posljedice odluke | Status odluke |
|-----------|-------|--------------|------|-------------------|-----------------|---------------|-------------------|---------------|
| DL-5.1 | 22.04.2026. | Odabir eksternog identity providera | Razmatran način upravljanja korisničkim identitetima i autentifikacijom. | Lokalno čuvanje lozinki (bcrypt); Keycloak kao eksterni identity provider | Keycloak kao eksterni identity provider | Centralizovano upravljanje korisnicima, podrška za OAuth 2.0/OIDC i buduću SSO integraciju bez lokalnog čuvanja lozinki. | Potrebno pokrenuti i konfigurisati Keycloak servis; backend zavisi od dostupnosti Keycloak instance. | Usvojena |
| DL-5.2 | 22.04.2026. | Odabir baze podataka | Razmatran izbor sistema za upravljanje relacijskom bazom podataka. | MySQL; PostgreSQL; SQL Server | MySQL | Dobra podrška za Docker, kompatibilnost s Prisma ORM-om i usklađenost s arhitekturalnom odlukom iz prethodnih sprintova. | Potrebno održavanje migracija; Prisma schema vezana za MySQL dialekt. | Usvojena |
| DL-5.3 | 22.04.2026. | Pristup bazi podataka | Razmatran način komunikacije s bazom podataka iz backend aplikacije. | Direktni SQL upiti; Prisma ORM | Prisma ORM | Prisma omogućava type-safe upite, automatsko generisanje klijenta i upravljanje migracijama bez pisanja raw SQL-a. | Potrebno generisati Prisma klijenta nakon svake promjene scheme; migracije moraju biti sinhronizovane s kodom. | Usvojena |
| DL-5.4 | 22.04.2026. | Organizacija arhitekture backend-a | Razmatran način organizacije backend aplikacije. | Monolit bez slojeva; Monolitna modularna (layered) arhitektura; Mikroservisi | Monolitna modularna arhitektura | Prikladan za sistem srednje složenosti; brži razvoj, lakše testiranje i jednostavnije održavanje u ranim fazama projekta. | Povećava inicijalnu kompleksnost strukture foldera; buduća migracija na mikroservise zahtijevala bi značajan refaktoring. | Usvojena |
| DL-5.5 | 22.04.2026. | Pokretanje razvojnog okruženja | Razmatran način standardizacije razvojnog okruženja za sve članove tima. | Lokalna instalacija servisa; Docker Compose | Docker Compose | Standardizovano okruženje za sve članove tima; ista konfiguracija MySQL, Keycloak i backend servisa na svim mašinama bez ručnog postavljanja. | Zahtijeva instaliran Docker; `docker-compose.yml` mora se održavati sinhronizovano s promjenama u projektu. | Usvojena |
| DL-5.6 | 22.04.2026. | Frontend arhitektura i odabir framework-a | Razmatran tip i tehnologija za izradu korisničkog interfejsa. | Multi-page aplikacija; React SPA; Next.js App Router | Next.js 15 (App Router) | App Router omogućava server i client komponente, optimizovano renderovanje i zaštitu ruta putem edge middleware-a bez dodatnih biblioteka. | `NEXT_PUBLIC_*` varijable moraju biti postavljene pri build-u; potrebno upravljanje state-om na klijentskoj strani. | Usvojena |
| DL-5.7 | 25.04.2026. | Čuvanje JWT tokena na frontend-u | Razmatran siguran način pohrane access tokena u browseru. | localStorage; sessionStorage; Memorija aplikacije; HTTP kolačić | HTTP kolačić s `withCredentials: true` | Kolačić se automatski šalje uz svaki zahtjev; čita ga i Next.js middleware i backend auth middleware iz `Cookie` headera. Manji rizik od XSS-a u odnosu na localStorage. | Logout mora eksplicitno brisati kolačić; backend i middleware moraju koristiti isto ime kolačića (`token`). | Usvojena |
| DL-5.8 | 25.04.2026. | ExternalIdentity model — provider-agnostic imenovanje | Razmatran način pohrane veze između lokalnog korisničkog profila i eksternog identity providera u bazi. | Keycloak-specifični atributi (npr. `keycloakId`); Provider-agnostic model (`provider`, `externalId`) | Provider-agnostic `ExternalIdentity` model | Fleksibilnost za dodavanje novih identity providera u budućnosti bez promjene schema strukture; smanjuje coupling između baze i konkretnog auth provajdera. | Mapiranje između Keycloak `sub`-a i lokalnog korisnika vrši se pri svakom autentifikovanom zahtjevu. | Usvojena |
| DL-5.9 | 25.04.2026. | Strategija seed podataka | Razmatran način kreiranja inicijalnih podataka za razvoj i demo okruženje. | Jednokratni `create` upiti; Idempotentni `upsert` s prirodnim ključevima; Hard-coded numerički ID-ovi | Idempotentni `upsert` s prirodnim ključevima | Seed skripta može se pokrenuti višekratno bez grešaka ili duplikata; prirodni ključevi ne ovise o stanju auto-increment sekvenci u bazi. | Seed mora ostati sinhronizovan s `schema.prisma`; svaka promjena auth modela zahtijeva ažuriranje seed-a i testova zajedno. | Usvojena |
| DL-5.10 | 26.04.2026. | Zaštita ruta na frontend-u | Razmatran mehanizam kontrole pristupa stranicama aplikacije. | Backend-only provjera pri svakom API pozivu; React ProtectedRoute komponenta; Next.js Middleware | Next.js Middleware (`middleware.ts`) | Middleware se izvršava na edge nivou prije renderovanja stranice; centralizovana logika za sve zaštićene rute bez omotač komponenti. | Javne rute moraju biti eksplicitno navedene u `PUBLIC_ROUTES` listi; middleware provjerava samo prisustvo kolačića, ne validira kriptografski potpis tokena. | Usvojena |
| DL-5.11 | 26.04.2026. | Naziv Keycloak realma | Razmatran naziv realma koji se koristi u svim env varijablama i API pozivima. | `servisni-sistem` (na bosanskom); `service-system` (na engleskom) | `service-system` | Tehnički identifikatori koriste engleski radi konzistentnosti s ostatkom stacka i izbjegavanja problema s enkodiranjem specijalnih znakova u URL-ovima. | Env varijabla `KEYCLOAK_REALM=service-system` mora biti postavljena u svim okruženjima (lokalno, Docker, cloud). | Usvojena |
| DL-5.12 | 26.04.2026. | Uklanjanje `companyId` s forme za registraciju | Razmatrano da li korisniku pri samoregistraciji treba ponuditi odabir ili unos kompanije. | Opcioni `companyId` na register formi; Potpuno uklanjanje polja | Potpuno uklanjanje `companyId` s register forme | Samoregistracija je namijenjena građanima koji prijavljuju kvarove i nisu vezani za kompaniju pri kreiranju računa; dodjela kompanije je administrativni tok. | Dodjela korisnika kompaniji mora biti riješena kroz zasebni administrativni flow u narednim sprintovima. | Usvojena |
| DL-5.13 | 29.04.2026. | Rate limiting — granularnost i biblioteka | Razmatran mehanizam i jedinica za ograničavanje broja zahtjeva na auth i javnim endpointima. | Po IP adresi (express-rate-limit); Po autentikovanom korisniku; Kombinovano | Rate limiting po IP adresi (`express-rate-limit`, 20 req / 15 min) | Primjenjivo i na neautentikovane zahtjeve (login, reset lozinke, javna prijava kvara); biblioteka već prisutna u projektnom stacku. | Nepouzdano iza NAT-a ili dijeljenog proxy-ja; trebalo bi preispitati ako se pojave problemi s lažnim blokadama u produkciji. | Usvojena |
| DL-5.14 | 29.04.2026. | CI/CD platforma za automatsku provjeru i deploy | Razmatrana platforma za automatizovanu provjeru koda na PR-ovima i isporuku u ciljana okruženja. | Ručni deploy; GitHub Actions + Cloudflare Pages + Railway; Self-hosted CI/CD | GitHub Actions + Cloudflare Pages (frontend) + Railway (backend) | Besplatni nivoi kompatibilni s projektnim budžetom; direktna GitHub integracija minimizuje konfiguraciju; `ci.yml` za PR provjere, `release.yml` za deploy na `release/*` i `master`. | `NEXT_PUBLIC_API_BASE_URL` i `NEXT_PUBLIC_SOCKET_URL` moraju biti postavljene u Cloudflare/Railway okruženju — bez toga frontend ostaje vezan za localhost. | Usvojena |
| DL-5.15 | 22.04.2026. | Razmatranje lokalne pohrane lozinki | Razmatrana mogućnost čuvanja hash-ovanih lozinki direktno u lokalnoj bazi podataka umjesto korištenja eksternog provajdera. | Lokalno čuvanje lozinki (bcrypt); Keycloak kao eksterni identity provider | Keycloak kao eksterni identity provider | Lokalna pohrana lozinki povećava sigurnosnu odgovornost tima; arhitekturalni dokument eksplicitno definira da aplikacija ne čuva lozinke niti lokalne uloge. | — | Neusvojena |
| DL-5.16 | 25.04.2026. | Razmatranje localStorage za čuvanje tokena | Razmatrana mogućnost pohrane JWT tokena u `localStorage` preglednika. | localStorage; HTTP kolačić | HTTP kolačić | `localStorage` je dostupan JavaScript kodu i podložan XSS napadima; kolačić s `withCredentials` pruža veću sigurnost uz automatsko slanje uz zahtjeve. | — | Neusvojena |
| DL-5.17 | 22.04.2026. | Razmatranje PostgreSQL kao baze podataka | Razmatrana mogućnost korištenja PostgreSQL umjesto MySQL kao primarnog RDBMS-a. | PostgreSQL; MySQL | MySQL | Arhitekturalna odluka donesena u Sprintu 3 definira MySQL kao ciljni RDBMS; prelaz bi zahtijevao izmjenu Prisma datasource konfiguracije i svih migracija. | — | Neusvojena |
| DL-5.18 | 28.04.2026. | Izvor korisničkih rola za autorizaciju | Razmatran način dohvatanja korisničkih rola za potrebe autorizacije u backend aplikaciji. | Ručno definisane role u lokalnoj bazi; Role u JWT tokenu; Direktni pozivi Keycloak API-ja | Role iz Keycloak access tokena | Role su već uključene u access token koji izdaje Keycloak, što omogućava efikasnu autorizaciju bez dodatnih API poziva i smanjuje latenciju. Također se zadržava konzistentnost između identity providera i backend sistema. | Backend mora parsirati i dekodirati JWT token kako bi izvukao role; sigurnosna validacija potpisa tokena trenutno nije u potpunosti implementirana i predstavlja potencijalni rizik. | Usvojena |